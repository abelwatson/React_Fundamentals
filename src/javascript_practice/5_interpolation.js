const username = 'Kenn';
console.log(`${username} is the JS Wizard`);

console.log(`Hey Students,
What Bugs can
${username}
fix
for you today?`);

const person = {
    username: 'Kenn',
    profession: 'Secret Agent'
};

console.log(`${person.username}, ${person.profession}, extraordinaire`)